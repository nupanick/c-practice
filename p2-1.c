/* p2-1.c
 * by Nicholas Lamicela, 2017
 * Calculates (a-b)(x-y) with hard-coded values for a,b,x,y.
 */

#include <stdio.h>

int main(){
    
    // hardcoded variables, oh well
    int a,b,x,y;
    a = 5;
    b = 1;
    x = 10;
    y = 5;

    printf("%d", (a-b)*(x-y));
}

